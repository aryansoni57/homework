#include<stdio.h>

int main()
{
    int a,b;
    scanf("%i",&a);
    scanf("%i",&b);
    if (a==b)
    {
        printf("No. are same");
    }
    else
    {
        printf("No are not same");
    }
    return 0;
}